# ==============================================================================
# .env.example - YOLO Runner Configuration
# ==============================================================================

# --- Organisation-Level Runner (runs jobs for any repo in org) ---
REPOSITORY=myorg
ACCESS_TOKEN=ghp_xxxxx  # Needs admin:org scope
RUNNER_NAME=yolo-org-runner # Creates runners like yolo-org-runner-a1b2c3d4e5f6
RUNNER_LABELS=self-hosted,linux,docker,yolo,org

# --- Repository-Level Runner (runs jobs for one specific repo) ---
# REPOSITORY=myorg/myrepo # Or myuser/myrepo
# ACCESS_TOKEN=ghp_xxxxx  # Needs repo scope
# RUNNER_NAME=yolo-repo-runner  # Creates runners like yolo-repo-runner-a1b2c3d4e5f6
# RUNNER_LABELS=self-hosted,linux,docker,yolo,repo

# ==============================================================================
# Usage:
#   1. Copy: cp .env.example .env
#   2. Uncomment and edit ONE configuration above
#   3. Start: docker-compose up -d
#   4. Scale: docker-compose up -d --scale gha-runner-yolo=10
#
# ==============================================================================